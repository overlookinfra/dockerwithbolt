pipeline {
    agent{
        label 'worker'
    }

    stages {
        stage('build') {
            steps {
                sh 'echo "hello world"'
                sh 'ls'
            }
        }
    }
}
